import React from 'react';

const reviewCart = (props) => {
    console.log(props.children[1])
    return (
        <div>
            <h2>This is ReviewCart Page</h2>
        </div>
    );
};

export default reviewCart;